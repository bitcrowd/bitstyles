import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta title="Base/Typography/Responsive Headings" />

# Typography

Using the default configuration provides responsive sizing for the heading elements `h1`, `h2`, `h3`, `h4`, `h5`, `h6`, and `body`, and `p` elements. In this context, “responsive font-sizing” means the elements have a default font-size that is overridden at larger breakpoints to become larger, making use of the extra space. As is also common, the ratio between font-sizes is larger on larger screens.

All font-sizes are taken from the base font-size design tokens specified in `settings/typography`. See [Design tokens/typography](/docs/DesignTokens/Typography) for details.

## Elements

Semantic headings from `<h1>` to `<h6>` will be sized as shown (resize your viewport to see how they change at different viewport sizes):

<Canvas isColumn>
  <Story name="h1">
    {`
      <h1>h1 sized text. Jelly cake marshmallow</h1>
    `}
  </Story>
  <Story name="h2">
    {`
      <h2>h2 sized text. Jelly cake marshmallow</h2>
    `}
  </Story>
  <Story name="h3">
    {`
      <h3>h3 sized text. Jelly cake marshmallow</h3>
    `}
  </Story>
  <Story name="h4">
    {`
      <h4>h4 sized text. Jelly cake marshmallow</h4>
    `}
  </Story>
  <Story name="h5">
    {`
      <h5>h5 sized text. Jelly cake marshmallow</h5>
    `}
  </Story>
  <Story name="h6">
    {`
      <h6>h6 sized text. Jelly cake marshmallow</h6>
    `}
  </Story>
  <Story name="p">
    {`
      <p>p sized text. Jelly cake marshmallow</p>
    `}
  </Story>
</Canvas>

## Customization

The `'base'` sizes shown below apply for all screensizes, until overridden at the `m` breakpoint (see [breakpoints](/docs/design-tokens-breakpoints--page)):

```scss
$font-sizes: (
  '#{setup.$no-media-query}': (
    'h1': font-size.get('6'),
    'h2': font-size.get('5'),
    'h3': font-size.get('4'),
    'h4': font-size.get('3'),
    'h5': font-size.get('2'),
    'h6': font-size.get('1'),
    'body': font-size.get('2'),
    'p': font-size.get('2'),
  ),
  'm': (
    'h1': font-size.get('8'),
    'h2': font-size.get('7'),
    'h3': font-size.get('6'),
    'h4': font-size.get('4'),
    'h5': font-size.get('2'),
    'h6': font-size.get('1'),
    'body': font-size.get('2'),
    'p': font-size.get('2'),
  ),
);
```
