import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta title="Utilities/shadow" />

# Drop-shadow

Apply a drop-shadow to the element, selected from the standard set of shadows.

## u-shadow

<Canvas isColumn>
  <Story name="u-shadow">
    {`
      <div class="u-shadow-default u-padding-m u-bg-white" style="width: 10rem; height: 10rem;">
      </div>
    `}
  </Story>
</Canvas>

## Customization

The available display types can be customized by overriding the `$bitstyles-display-values` Sass variable:

```scss
$bitstyles-display-values: (
  'block': block,
  'grid': grid,
  'flex': flex,
  'inline-flex': inline-flex,
  'hidden': none
);
```

The breakpoints these classes are available at can be customized by overriding the `$bitstyles-display-breakpoints` variable:

```scss
$bitstyles-display-breakpoints: ('s', 'm', 'xl');
```
