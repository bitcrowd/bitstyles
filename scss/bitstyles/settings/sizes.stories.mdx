import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta title="Design Tokens/Sizes" />

# Sizes

Using a central scale of sizes maintains consistency in spacing (most often padding and margins) throughout your project. The default sizes are based on font-size to keep the spacing tied to the typography. For users who view your content at a different font-size, the spacing will remain proportional and usable.

The default size scale:

<Canvas>
  <Story name="Sizes">
    {`
      <div class="u-flex u-items-end u-gap-s4">
        <div class="u-shadow-default u-bg-gray-dark u-border-radius-s7" style="width: var(--bs-size-s7); height: var(--bs-size-s7)"></div>
        <div class="u-shadow-default u-bg-gray-dark u-border-radius-s7" style="width: var(--bs-size-s6); height: var(--bs-size-s6)"></div>
        <div class="u-shadow-default u-bg-gray-dark u-border-radius-s7" style="width: var(--bs-size-s4); height: var(--bs-size-s4)"></div>
        <div class="u-shadow-default u-bg-gray-dark u-border-radius-s7" style="width: var(--bs-size-s); height: var(--bs-size-s)"></div>
        <div class="u-shadow-default u-bg-gray-dark u-border-radius-s7" style="width: var(--bs-size-m); height: var(--bs-size-m)"></div>
        <div class="u-shadow-default u-bg-gray-dark u-border-radius-s7" style="width: var(--bs-size-l); height: var(--bs-size-l)"></div>
        <div class="u-shadow-default u-bg-gray-dark u-border-radius-s7" style="width: var(--bs-size-l3); height: var(--bs-size-l3)"></div>
        <div class="u-shadow-default u-bg-gray-dark u-border-radius-s7" style="width: var(--bs-size-l4); height: var(--bs-size-l4)"></div>
        <div class="u-shadow-default u-bg-gray-dark u-border-radius-s7" style="width: var(--bs-size-l5); height: var(--bs-size-l5)"></div>
        <div class="u-shadow-default u-bg-gray-dark u-border-radius-s7" style="width: var(--bs-size-l6); height: var(--bs-size-l6)"></div>
        <div class="u-shadow-default u-bg-gray-dark u-border-radius-s7" style="width: var(--bs-size-l7); height: var(--bs-size-l7)"></div>
      </div>
    `}
  </Story>
</Canvas>

```scss
$sizes: (
  '0': 0,
  's7': 0.125rem, // 2px
  's6': 0.25rem, // 4px
  's4': 0.45rem, // 8px
  's': 0.75rem, // 12px
  'm': 1rem, // 16px
  'l': 1.5rem, // 24px
  'l3': 2rem, // 32px
  'l4': 2.5rem, // 40px
  'l5': 3rem, // 48px
  'l6': 4rem, // 64px
  'l7': 5rem, // 80px
);
```

## Using the sizes in Sass

Use the `size.get()` helper function for accessing sizes in the sizing scale. In your component Sass file first import the `size` tools, then use the `get()` function, passing the size name:

```scss
@use '~bitstyles/scss/bitstyles/tools/size';

.component-class::after {
  margin-left: size.get('l3');
}
```

With the default setup this will output:

```css
.component-class::after {
  margin-left: 1.6rem;
}
```

### `size.get()` parameters

| Name  | Type   | Description          |
| ----- | ------ | -------------------- |
| $size | string | The name of the size |

## Using the sizes in classes

Use the appropriate utility classes to apply spacing. See the documentation of each class for information on defaults & customization:

- [`u-margin`](/docs/utilities-margin--margin-0)
- [`u-padding`](/docs/utilities-padding--padding-0)

## Using the sizes in CSS

The sizing scale is also available as CSS custom properties. In the default configuration, they are namespaced with `bs`, see [setup](/story/design-tokens-setup--page):

```css
--bs-size-s7: 0.125rem;
--bs-size-s6: 0.25rem;
--bs-size-s4: 0.5rem;
--bs-size-s: 0.75rem;
--bs-size-m: 1rem;
--bs-size-l: 1.5rem;
--bs-size-l3: 2rem;
--bs-size-l4: 2.5rem;
--bs-size-l5: 3rem;
--bs-size-l6: 4rem;
--bs-size-l7: 5rem;
```

## Using size design tokens in JS

The size design tokens can be imported and used in JS:

```js
import sizes from '../exports/_sizes.scss';

console.log(sizes['0']); // '0'
console.log(sizes['l4']); // '2.5rem'
console.log(sizes['l3']); // '2rem'
console.log(sizes['l']); // '1.5rem'
// …
```

## Customization

### Base size

The size scale is computed from a single starting value. By changing that starting value you can change the whole scale:

```scss
@use '~bitstyles/scss/bitstyles/settings/layout' with (
  $size-base: 2rem
);

/* sizes:

(
  "0": 0,
  "s7": 0.25rem,
  "s6": 0.5rem,
  "xs": 1rem,
  "s": 1.5rem,
  "m": 2rem,
  "l": 3rem,
  "xl": 4rem,
  "l4": 5rem,
  "l5": 6rem,
  "l6": 8rem,
  "l7": 10rem,
)
*/
```

### Overriding the size scale

When you need to increase or reduce the number of sizes available, change the names of the sizes, adjust the ratio between each point of the scale, or otherwise have total control, you’ll need to override the `$sizes` Sass map. Note that you cannot add to nor edit the values in the default map. You can only overwrite the entire thing, so be sure to copy the sizes you’ll use even if you don’t change them.

```scss
@use '~bitstyles/scss/bitstyles/settings/layout' with (
  $sizes: (
    '0': 0,
    // add some pixel-based sizes for small adjustments
    '1': 1px,
    's7': 0.125rem,
    's6': 0.25rem,
    's4': 0.5rem,
    's': 0.75rem,
    'm': 1rem,
    'l': 1.25rem,
    'l3': 2rem,
    'l4': 4rem,
    'l5': 8rem,
  )
);
```
