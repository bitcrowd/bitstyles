import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta title="Atoms/Button/Grouped" />

# Grouped Buttons

A set of buttons that will appear to be joined to other `.a-button--grouped`s siblings.

There are three button components: first, last, and middle.

<Canvas>
  <Story name="Grouped__first">
    {`
      <button type="button" class="a-button a-button--grouped a-button--grouped__first">
        First
      </button>
    `}
  </Story>
  <Story name="Grouped">
    {`
      <button type="button" class="a-button a-button--grouped">
        Middle
      </button>
    `}
  </Story>
  <Story name="Grouped__last">
    {`
      <button type="button" class="a-button a-button--grouped a-button--grouped__last">
        Last
      </button>
    `}
  </Story>
</Canvas>

When you’re representing state in the UI, use the `aria-pressed="true"` attribute to indicate the currently-pressed button.

<Canvas>
  <Story name="Button--grouped [aria-pressed]">
    {`
      <button type="button" class="a-button a-button--grouped u-h6" aria-pressed="true">
        Pressed
      </button>
    `}
  </Story>
</Canvas>
