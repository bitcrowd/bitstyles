import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';
import icons from '../../../../assets/images/icons.svg';

<Meta title="Atoms/Button/Icon" />

# Icon Button

A specialised button type where the visible content is intended to be only an icon. Commonly used for `+`, `<`, and `>` buttons, share buttons (facebook, twitter etc.).

Always provide:

- **a `title` attribute**. When a user hovers their pointer over the button, they’ll be told what it does (icons alone are not always clear).
- **a `sr-only` label** containing descriptive text next to the icon, so the button will have valid text to use as button label (see examples). This dictates whether people with visual impairments (whether temporary or permanent) can use our UIs.


Here’s a base icon button:

<Canvas>
  <Story name="Button element">
    {`
      <button class="a-button a-button--icon" title="Button element">
        <svg width="20" height="20" class="a-icon" aria-hidden="true" focusable="false">
          <use xlink:href="${icons}#icon-plus"></use>
        </svg>
        <span class="u-sr-only">Button element</span>
      </button>
    `}
  </Story>
</Canvas>

Here’s the same button in the disabled state:

<Canvas>
  <Story name="Button element (disabled)">
    {`
      <button disabled class="a-button a-button--icon" title="Button element (disabled)">
        <svg width="20" height="20" class="a-icon" aria-hidden="true" focusable="false">
          <use xlink:href="${icons}#icon-plus"></use>
        </svg>
        <span class="u-sr-only">Button element (disabled)</span>
      </button>
    `}
  </Story>
</Canvas>

This shows there’s no visual difference when using an anchor element. Note there’s no disabled state for such buttons, as that doesn’t really exist in the HTML spec. The second button here is visited, showing there’s no visual difference:

<Canvas>
  <Story name="Anchor element as button">
    {`
      <a href="#" class="a-button a-button--icon" title="Anchor element">
        <svg width="20" height="20" class="a-icon" aria-hidden="true" focusable="false">
          <use xlink:href="${icons}#icon-plus"></use>
        </svg>
        <span class="u-sr-only">Anchor element</span>
      </a>
    `}
  </Story>
  <Story name="Anchor element as button (visited)">
    {`
      <a href="/" class="a-button a-button--icon" title="Anchor element (visited)">
        <svg width="20" height="20" class="a-icon" aria-hidden="true" focusable="false">
          <use xlink:href="${icons}#icon-plus"></use>
        </svg>
        <span class="u-sr-only">Anchor element (visited)</span>
      </a>
    `}
  </Story>
</Canvas>
