import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';

<Meta title="Layout/Fixed ratio" />

# Fixed-ratio

Enforce an aspect ratio on this element and its contents.

The fixed-ratio element takes 100% of its parent element’s width, then the height is set so the element will maintain the specified ratio. The default ratio is 16:9, defined in the Sass map `$fixed-ratio-default-ratio` under the keys `width` and `height` e.g.

```scss
$fixed-ratio-default-ratio: (
  'width': 16,
  'height': 9
)
```

No content may overflow this container.

<Canvas>
  <Story name="Fixed ratio">
    {`
      <div class="l-fixed-ratio">
        <div class="l-fixed-ratio__inner u-background-color--secondary">
          <div class="l-absolute-center u-text-align--center">16:9 <span class="l-block">(try resizing)</span></div>
        </div>
      </div>
    `}
  </Story>
</Canvas>
